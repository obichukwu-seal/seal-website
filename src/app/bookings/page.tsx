import { BookConsultation } from "@/components";
import React from "react";

type Props = {};

const Bookings = (props: Props) => {
  return (
    <main>
      <BookConsultation />
    </main>
  );
};

export default Bookings;
