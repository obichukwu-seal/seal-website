import { ContactUsPageContent } from "@/components";
import React from "react";

type Props = {};

const ContactUs = (props: Props) => {
  return (
    <main>
      <ContactUsPageContent />
    </main>
  );
};

export default ContactUs;
